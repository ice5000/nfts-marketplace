import React from "react";
import { MagnifyingGlassIcon } from "@heroicons/react/24/solid";

const Header = ({ user }) => {

  return (
    <div>
    </div>
  );
};

Header.defaultProps ={
    user: 'Diffrent from Balabulu'
}

export default Header;
